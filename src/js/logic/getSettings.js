
/*export const getSettings = (currSettings) => {
  const localSettings = localStorage.getItem('settings');
  currSettings = JSON.parse(localSettings);
  console.log(`currSettt - ${currSettings}`)
  return currSettings;
}

export default getSettings;*/